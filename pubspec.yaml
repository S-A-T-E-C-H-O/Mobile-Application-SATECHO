name: satecho_mobile
description: SATECHO mobile application for farmer workflows.
publish_to: "none"
version: 0.1.0+2

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.0
  connectivity_plus: ^6.1.0
  share_plus: ^10.1.0
  printing: ^5.13.0
  image_picker: ^1.1.2
  geolocator: ^13.0.0
  fl_chart: ^0.69.0
  local_auth: ^2.3.0
  firebase_messaging: ^15.1.3
  firebase_core: ^3.6.0
  mqtt_client: ^10.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_native_splash: ^2.4.7

flutter:
  uses-material-design: true

flutter_native_splash:
  color: "#ffffff"
  image: assets/splash.png
  android_12:
    image: assets/splash.png
    color: "#ffffff"

